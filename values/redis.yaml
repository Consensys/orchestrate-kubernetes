---
fullnameOverride: redis-nonce
usePassword: false
master:
  persistence:
    enabled: false
configmap: |-
  notify-keyspace-events Kg
  maxmemory 100mb
  maxmemory-policy allkeys-lru
cluster:
  enabled: false
metrics:
  enabled: false
